import { PageHeader } from "../components/Headers"

const ArcadePage = () => <div>
  <PageHeader className="mb-4">Arcade</PageHeader>
  <div>Sometimes when I have a spare few hours, I like to tinker around making basic games in React. I don't have anywhere else to put them, so I might as well put them here.</div>
</div>

export default ArcadePage